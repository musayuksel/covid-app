function YAxis({gridArea}) {
    const styles = {
        gridArea: gridArea,
        width: "100%",
        height: "100%",
        background: "red",
    }
    
    return (
        <svg style={styles}></svg>
    )
}

export default YAxis